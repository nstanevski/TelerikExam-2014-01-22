using System;
using System.Collections.Generic;
using System.Text;
using System.Numerics;

class Bunny_factory
{
    static void Main()
    {
        List<int> factory = new List<int>();
        string input = "";
        while ((input = Console.ReadLine()) != "END")
        {
            factory.Add(int.Parse(input));
        }
        //foreach (int cage in factory)
        //{
        //    Console.WriteLine(cage);
        //}
        while (true)
        {

        }
    }
}